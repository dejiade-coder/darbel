# =============================================================================
# Darbel Frontend — Environment Variables
# =============================================================================

# Base URL of the Darbel backend API (includes the /api/v1 prefix).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1

# Set to 'production' in real deployments. Cookies are marked Secure when NODE_ENV=production.
NODE_ENV=development
